\chapter{系统实现与实验评价}\label{chap:evaluation}

本章主要阐述基于 FPGA 的 SLAM 后端加速系统的实验平台与环境搭建，并通过一系列定量实验验证系统的有效性。实验评估围绕轨迹精度、计算延迟与加速比、资源利用率、功耗与能效以及 SOB 编码优化效果展开，并在公开数据集与真实场景下进行综合验证。

\section{实验平台与环境搭建}

本节详细介绍实验所采用的硬件平台配置、软件系统架构设计以及用于验证的数据集基准。为了验证软硬协同加速方案的有效性，实验构建了一套涵盖底层 FPGA 逻辑、嵌入式运行时环境与上层应用框架的完整测试系统。

\subsection{硬件平台配置}

本文实验平台采用赛灵思（Xilinx）Zynq UltraScale+ MPSoC 系列开发板，覆盖实验评估与真实场景验证两类硬件环境。整体硬件系统的 Block Design 遵循高吞吐量、低延迟的设计原则，通过 AXI 高性能总线实现 PS 端与 PL 端的紧耦合互联。

\subsubsection{评估平台 (ZCU106)}

离线性能评估与资源统计在 ZCU106 开发板上完成（如图~\ref{fig:hetero_platform} 所示）。该平台集成四核 ARM Cortex-A53 处理器与大规模可编程逻辑资源，能够稳定支撑后端加速器的软硬协同验证。ZCU106 平台的主要硬件配置如下：
\begin{figure}[h!t]
\centering
\includegraphics[width=0.9\linewidth]{Img/zcu-106.jpg}
\bicaption{评估平台 ZCU106 实物图}{Physical view of the ZCU106 evaluation platform}
\label{fig:hetero_platform}
\end{figure}
\begin{itemize}
    \item 处理器系统（PS）：四核 ARM Cortex-A53，最高频率 1.2~GHz，配备 4~GB DDR4 内存。
    \item 可编程逻辑（PL）：约 1143K 逻辑单元（Logic Cells）与 2520 个 DSP 计算切片。
\end{itemize}

\subsubsection{应用平台 (XCZU19EG)}

真实场景测试部分使用集成式异构计算板卡（如图~\ref{fig:application_platform} 所示），其核心器件为 XCZU19EG。该平台在 PS 端同样采用 Cortex-A53 架构，PL 端提供更充足的逻辑单元与片上存储资源。为了满足真实场景下高动态定位的需求，板卡集成了高性能的视觉与惯性传感器：

\begin{figure}[h!t]
\centering
\includegraphics[width=0.9\linewidth]{Img/application_platform.png}
\bicaption{异构计算应用平台 (XCZU19EG) 实物图}{Physical view of the XCZU19EG application platform}
\label{fig:application_platform}
\end{figure}

\begin{itemize}
    \item \textbf{视觉传感器}：采用安森美（ON Semiconductor）AR0144AT 图像传感器。该传感器为 1/4 英寸 1.0 MP CMOS，分辨率为 $1280 \times 800$。其核心特性是采用全局快门（Global Shutter）像素设计，专为捕捉高速运动场景优化，能够产生清晰、低噪声的图像，有效避免卷帘快门带来的几何畸变，确保前端特征追踪的精度。双目相机模组的物理基线长度（Baseline）设置为 100~mm，以平衡深度估计的精度与测量范围。
    \item \textbf{惯性测量单元（IMU）}：选用 TDK InvenSense ICM-20948 芯片。这是一款超低功耗的 9 轴运动追踪设备，集成 3 轴陀螺仪、3 轴加速度计和 3 轴磁力计。它内置 16 位 ADC，支持可编程数字滤波器，并通过 7~MHz 高速 SPI 接口与 FPGA 通信，为 VIO 系统提供高频且精确的惯性测量数据。
\end{itemize}

值得注意的是，该平台通过 PL 端逻辑实现了 FSYNC 硬件同步：将按照 IMU 采样时钟生成的 FSYNC 触发信号直接连接至图像传感器的外触发引脚，使图像采集的曝光中心时刻与 IMU 的采样时刻在硬件微秒级层面对齐。该机制极大地减小了视觉与惯性数据之间的时间偏移（Time Offset），为后端基于 Schur Complement 的紧耦合位姿估计提供了高质量、时间同步的观测数据输入。

两类平台均采用统一的软硬划分方式，保证实验结果的可比性。

\subsection{软件系统架构}

为了提高开发效率并实现算法的灵活部署，软件环境基于 PYNQ 3.0.1 框架构建。PYNQ 利用 Python 的生产力优势与 FPGA 的硬件性能，通过 Overlay 机制实现比特流的动态加载与 API 封装。系统的软件栈架构设计如图~\ref{fig:software_arch} 所示，具体包括：

\begin{figure}[h!t]
    \centering
    \includegraphics[width=0.95\linewidth]{Img/software_arch_system.pdf}
    \bicaption{基于 PYNQ 与 Docker 的软件系统架构}{Software system architecture based on PYNQ and Docker}
    \label{fig:software_arch}
\end{figure}

\begin{itemize}
    \item \textbf{操作系统与容器化部署}：宿主机运行基于 PYNQ 3.0.1 的 Linux 系统。为隔离开发环境与依赖库，采用 Docker 技术构建了轻量级且独立的运行环境。容器内部署 Ubuntu 20.04 LTS 镜像，并预装了 ROS、Ceres Solver、Eigen 等 SLAM 核心依赖库。
    \item \textbf{SchurVINS 算法框架集成}：系统选用 SchurVINS 作为 VIO 算法载体。SchurVINS 是一种基于舒尔补的高效视觉惯性里程计框架\cite{fan2024schurvins}，天然契合本文提出的后端加速策略。在移植过程中，我们将原有的 CPU 端舒尔消元线程替换为对 FPGA 加速器的调用接口。
    \item \textbf{ROS 节点管理}：通过 ROS1 Noetic 进行系统层面的节点管理与通信。系统主要包含传感器驱动节点、前端特征追踪节点与后端优化节点。各节点间通过 ROS Topic 传输图像与 IMU 消息，保证了模块间的解耦。
    \item \textbf{交叉编译与仿真}：为了在 x86 主机上加速嵌入式代码的开发，利用 QEMU 模拟器搭建了 ARM64 虚拟环境。该环境允许在 PC 端验证算法逻辑与库兼容性，随后通过交叉编译工具链生成目标平台的可执行文件。
\end{itemize}

\subsection{数据集与测试基准}

软件基准在编译时开启 \texttt{-O3} 级别优化，以确保对比公平性。实验数据采用 EuRoC MAV 数据集与 TUM-VI 数据集，二者均提供精确地面真值轨迹，可用于定量评估系统的定位精度与计算延迟。

EuRoC MAV 数据集由微型飞行器在室内工业环境中采集，提供已同步的双目图像、IMU 测量与高精度轨迹真值\cite{Burri25012016}。数据集包含 11 个工厂场景序列，覆盖从光照良好且运动平缓到运动模糊与弱纹理条件下的快速机动，能够系统性评估视觉惯性系统在复杂动态条件下的精度与鲁棒性。序列命名中的 easy、medium 与 difficult 分别对应不同的飞行难度等级，难度越高意味着更剧烈的运动与更不利的视觉条件。表~\ref{tab:euroc_sequences} 给出了各序列的轨迹长度统计，用于后续误差归一化与性能对比。

\begin{table}[htbp]
\centering
\bicaption{EuRoC MAV 数据集序列与轨迹长度}{EuRoC MAV sequences and trajectory lengths}
\label{tab:euroc_sequences}
\scriptsize
\begin{tabular}{lccccccccccc}
\toprule
序列名称 & MH\_01 & MH\_02 & MH\_03 & MH\_04 & MH\_05 & V1\_01 & V1\_02 & V1\_03 & V2\_01 & V2\_02 & V2\_03 \\
\midrule
轨迹长度（m） & 80.6 & 73.5 & 130.9 & 91.7 & 97.6 & 58.6 & 75.9 & 79.0 & 36.5 & 83.2 & 86.1 \\
\bottomrule
\end{tabular}
\end{table}

TUM-VI 数据集覆盖多种室内场景与运动模式，是面向视觉惯性里程计评测的公开基准\cite{schubert2018vidataset,klenk2021tumvie}。数据集提供 20~Hz 的相机序列与 200~Hz 的 IMU 三轴加速度/角速度测量，传感器在硬件层完成时间同步，并给出经过校准的光度与时间戳信息。为轨迹评估，数据集提供高频率的运动捕捉真值，并将其与相机与 IMU 测量精确对齐，从而支持高精度的轨迹误差分析。本文使用的图像分辨率为 512\,$\times$\,512，以适配嵌入式平台的带宽与计算约束。

TUM-VI 序列包含房间与走廊等典型室内场景，其中房间序列在全程提供真值轨迹，走廊序列在起止段提供真值轨迹，并覆盖不同纹理与视野结构。本文仅选取其中的走廊与室内场景序列用于测试，以覆盖结构化长廊与一般室内空间两类典型环境，从而验证系统在不同纹理与几何布局下的稳定性。

\subsection{传感器标定方法与参数配置}

为保证公开数据集评测与真实平台实验的标定一致性，本节对所用传感器标定方法与结果参数进行统一说明。数据集实验中，系统直接读取 EuRoC 和 TUM-VI 官方提供的标定文件；真实平台实验中，则需要对 XCZU19EG 平台上的双目相机与 ICM-20948 IMU 进行离线标定，以获得前端去畸变、双目三角化及视觉惯性预积分所需的全套参数。上述参数直接决定系统定位精度与数值稳定性，是保障真实平台实验可复现性的基础。

\subsubsection{标定框架与标定靶}

针对 XCZU19EG 平台的离线标定，本文采用 ETH-ASL 开发的开源标定工具 Kalibr\cite{furgale2013unified,rehder2016extending}。Kalibr 基于非线性批量优化方法，将标定板角点的视觉重投影误差与 IMU 测量残差作为联合优化目标，能够在单次离线标定流程中同时估计相机内参、双目外参、相机-IMU 空间变换以及时间偏移量，适用于视觉惯性系统的高精度传感器标定。

标定所用靶标为 AprilTag 编码标定板，规格为 $6\times6$ 编码阵列，总尺寸约为 $80\,\mathrm{cm}\times80\,\mathrm{cm}$，如图~\ref{fig:apriltag_board} 所示。AprilTag 标定板各编码块具有唯一标识符，支持在部分遮挡或运动模糊条件下进行可靠角点检测，相比传统棋盘格标定板具有更强的鲁棒性，尤其适合相机-IMU 联合标定时存在较大运动激励的场景。整体标定流程如图~\ref{fig:xczu19eg_calib_flow} 所示，分为双目相机内外参标定、相机-IMU 联合标定、IMU 噪声参数标定三个阶段。

\begin{figure}[h!t]
\centering
\includegraphics[width=0.50\linewidth]{Img/april_6x6_80x80cm_A0}
\captionsetup{justification=centering}
\bicaption{标定所用 AprilTag 标定板（$6\times6$ 编码阵列，约 $80\,\mathrm{cm}\times80\,\mathrm{cm}$）}{AprilTag calibration board used in this work ($6\times6$ tag array, approximately $80\,\mathrm{cm}\times80\,\mathrm{cm}$)}
\label{fig:apriltag_board}
\end{figure}

\begin{figure}[h!t]
\centering
\resizebox{0.88\linewidth}{!}{\input{Img/xczu19eg_calib_flow.tex}}
\bicaption{XCZU19EG 平台传感器标定流程}{Sensor calibration workflow for the XCZU19EG platform}
\label{fig:xczu19eg_calib_flow}
\end{figure}

\subsubsection{双目相机内外参标定}

第一阶段使用 Kalibr 的相机标定功能，以棋盘格靶标（$8\times6$ 内角点网格，间距 $4\,\mathrm{cm}$）对双目模组进行内外参标定。采集约 $144\,\mathrm{s}$ 的双目图像序列，每路相机共约 $2301$ 帧，采集时需保证标定板覆盖图像边缘区域，并在多种深度、俯仰与旋转姿态下进行，以充分激励各参数的可观测性。标定采用 pinhole-radtan 相机模型，同时估计左右相机的焦距、主点、径向-切向畸变系数及双目基线外参。

标定完成后，cam0 重投影误差均值为 $0.97\,\mathrm{px}$，cam1 为 $1.01\,\mathrm{px}$，双目基线约为 $9.92\,\mathrm{cm}$。图~\ref{fig:calib_polar_error} 给出了 cam0 重投影误差关于极角的分布，可见误差在大极角区域略有增大，但整体分布集中于 $0.15\,\mathrm{px}$ 以下，验证了标定结果的可靠性。图~\ref{fig:calib_cam_system} 给出了标定后双目相机坐标系的三维位置关系，cam0 与 cam1 的空间位姿估计与硬件水平安装方式一致，基线方向沿 $x$ 轴正方向，与平台实际安装相符。

\begin{figure}[h!t]
\centering
\includegraphics[width=0.88\linewidth]{Img/calib_cam_polar_error}
\bicaption{双目相机标定重投影误差关于极角的分布（cam0）}{Polar reprojection error distribution of stereo camera calibration (cam0)}
\label{fig:calib_polar_error}
\end{figure}

\begin{figure}[h!t]
\centering
\includegraphics[width=0.55\linewidth]{Img/calib_cam_params}
\bicaption{标定后双目相机坐标系三维位置关系（cam0 与 cam1）}{3D visualization of calibrated stereo camera coordinate frames (cam0 and cam1)}
\label{fig:calib_cam_system}
\end{figure}

\subsubsection{相机-IMU 联合标定}

第二阶段以双目标定结果为初值，使用 Kalibr 的相机-IMU 联合标定功能估计相机到 IMU 的刚体变换矩阵 $\mathbf{T}_{IC}$ 及时间偏移量。采集约 $148\,\mathrm{s}$ 的双目-IMU 同步数据序列，每路相机约 $2361$ 帧，IMU 记录共 $33375$ 条。采集过程中，将平台进行充分的六自由度激励，包含沿三轴的平移与绕三轴的旋转运动，以保证相机-IMU 外参及时间偏移的可观测性。Kalibr 对视觉重建轨迹与惯性积分轨迹进行联合批量优化，标定所用激励轨迹如图~\ref{fig:calib_imu_poses} 所示。

\begin{figure}[h!t]
\centering
\includegraphics[width=0.70\linewidth]{Img/calib_imu_poses}
\bicaption{相机-IMU 联合标定中 IMU 运动轨迹估计（六自由度激励）}{Estimated IMU trajectory during camera-IMU joint calibration (6-DoF excitation)}
\label{fig:calib_imu_poses}
\end{figure}

标定完成后，cam0 归一化重投影残差为 $0.97\,\mathrm{px}$，cam1 为 $1.01\,\mathrm{px}$；陀螺仪归一化残差为 $1.34$，加速度计归一化残差为 $1.55$，均接近理想值 $1.0$，表明 IMU 噪声模型与运动轨迹具有良好的一致性。相机时间偏移量 cam0 为 $13.5\,\mathrm{ms}$，cam1 为 $8.8\,\mathrm{ms}$，可在系统时间戳对齐中予以补偿。

\subsubsection{IMU 噪声参数标定}

Allan 方差（Allan Variance，AVAR）最初由 Allan 于 1966 年提出，用于表征原子频率标准的稳定性\cite{allan1966statistics}，后被广泛引入惯性传感器噪声分析领域\cite{elsheimy2008allan}。其核心思想是：将惯性测量信号按不同的积分时间 $\tau$（簇时间）分段平均后，计算相邻段均值之差的方差，从而揭示信号在不同时间尺度上的随机特性。Allan 偏差（Allan Deviation，ADEV）定义为：
\begin{equation}
\sigma(\tau) = \sqrt{\frac{1}{2}\left\langle\left(\bar{\Omega}_{k+1} - \bar{\Omega}_k\right)^2\right\rangle}
\label{eq:allan_dev}
\end{equation}
其中 $\bar{\Omega}_k$ 为第 $k$ 个时间窗口内传感器输出的均值，$\tau$ 为窗口长度。在双对数坐标下，$\sigma(\tau)$--$\tau$ 曲线的不同斜率区间对应不同类型的噪声：斜率为 $-1/2$ 的区间对应角度/速度随机游走（白噪声），代表传感器噪声密度；斜率为 $+1/2$ 的区间对应偏置随机游走（积分白噪声），代表偏置不稳定性对长时积分的影响。由此可直接从曲线中读取陀螺仪噪声密度 $\sigma_\mathrm{gyr}$（在 $\tau=1\,\mathrm{s}$ 处的 ADEV 值）和偏置随机游走系数 $\sigma_{\mathrm{gyr\_w}}$，加速度计参数同理。

第三阶段将平台保持静止，采集长时间惯性测量数据，利用上述 Allan 方差分析方法估计 ICM-20948 陀螺仪与加速度计的随机噪声特性。静置条件可消除真实运动引入的确定性分量，使随机噪声特性得以充分暴露。对 Allan 偏差曲线进行分段识别后，分别提取陀螺仪噪声密度 $\sigma_\mathrm{gyr}$、陀螺仪偏置随机游走 $\sigma_{\mathrm{gyr\_w}}$、加速度计噪声密度 $\sigma_\mathrm{acc}$ 以及加速度计偏置随机游走 $\sigma_{\mathrm{acc\_w}}$。这些参数将用于 VIO 系统中 IMU 预积分协方差传播与状态预测噪声建模，是保障真实平台定位精度的关键基础。

\subsubsection{标定参数汇总}

表~\ref{tab:euroc_calib} 与表~\ref{tab:tumvi_calib} 分别给出了 EuRoC 和 TUM-VI 数据集实验中使用的双目标定参数。两组参数均包含左右相机内参、畸变系数以及相机相对于机体系的外参矩阵，用于完成前端图像矫正、双目匹配和视觉观测建模。

\begin{table*}[!t]
\centering
\bicaption{EuRoC 数据集实验使用的双目标定参数}{Stereo calibration parameters used for EuRoC dataset experiments}
\label{tab:euroc_calib}
\scriptsize
\begin{tabular}{lcc}
\toprule
参数项 & cam0 & cam1 \\
\midrule
图像分辨率 & $752 \times 480$ & $752 \times 480$ \\
相机模型 & pinhole & pinhole \\
畸变模型 & radial-tangential & radial-tangential \\
$f_x$ & 458.6549 & 457.5874 \\
$f_y$ & 457.2967 & 456.1344 \\
$c_x$ & 367.2158 & 379.9994 \\
$c_y$ & 248.3753 & 255.2382 \\
$k_1$ & -0.283408 & -0.283684 \\
$k_2$ & 0.073959 & 0.074513 \\
$p_1$ & 0.000194 & -0.000105 \\
$p_2$ & 0.000018 & -0.000036 \\
$\mathbf{t}_{B\rightarrow C}$ (m) & $[-0.02164, -0.06468, 0.00981]^\mathrm{T}$ & $[-0.01984, 0.04537, 0.00786]^\mathrm{T}$ \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[!t]
\centering
\bicaption{TUM-VI 数据集实验使用的双目标定参数}{Stereo calibration parameters used for TUM-VI dataset experiments}
\label{tab:tumvi_calib}
\scriptsize
\begin{tabular}{lcc}
\toprule
参数项 & cam0 & cam1 \\
\midrule
图像分辨率 & $512 \times 512$ & $512 \times 512$ \\
相机模型 & pinhole & pinhole \\
畸变模型 & equidistant & equidistant \\
$f_x$ & 190.9785 & 190.4424 \\
$f_y$ & 190.9733 & 190.4344 \\
$c_x$ & 254.9317 & 252.5995 \\
$c_y$ & 256.8974 & 254.9172 \\
$k_1$ & 0.003482 & 0.003400 \\
$k_2$ & 0.000715 & 0.001766 \\
$k_3$ & -0.002053 & -0.002663 \\
$k_4$ & 0.000203 & 0.000330 \\
$\mathbf{t}_{B\rightarrow C}$ (m) & $[0.04557, -0.07116, -0.04468]^\mathrm{T}$ & $[-0.05546, -0.06925, -0.04745]^\mathrm{T}$ \\
\bottomrule
\end{tabular}
\end{table*}

针对 XCZU19EG 异构应用平台，系统运行时同时依赖双目相机参数与 IMU 参数。其中，相机参数给出了双目模组的内参、畸变参数以及相机相对于机体系的外参；IMU 参数则给出了 ICM-20948 的噪声密度与随机游走参数，用于后端预积分与状态传播。表~\ref{tab:xczu19eg_camera_calib} 与表~\ref{tab:xczu19eg_imu_calib} 汇总了真实平台实验中使用的关键标定结果。

\begin{table*}[!t]
\centering
\bicaption{XCZU19EG 平台双目相机标定参数}{Stereo camera calibration parameters on the XCZU19EG platform}
\label{tab:xczu19eg_camera_calib}
\scriptsize
\begin{tabular}{lcc}
\toprule
参数项 & cam0 & cam1 \\
\midrule
图像分辨率 & $640 \times 480$ & $640 \times 480$ \\
相机模型 & pinhole & pinhole \\
畸变模型 & radial-tangential & radial-tangential \\
$f_x$ & 483.6329 & 481.0721 \\
$f_y$ & 483.6221 & 481.0866 \\
$c_x$ & 333.1253 & 330.9346 \\
$c_y$ & 268.5127 & 241.2267 \\
$k_1$ & -0.395843 & -0.388561 \\
$k_2$ & 0.177872 & 0.159592 \\
$p_1$ & -0.000022 & -0.000068 \\
$p_2$ & 0.000195 & -0.000179 \\
$\mathbf{t}_{B\rightarrow C}$ (m) & $[0.01881, 0.02600, 0.13398]^\mathrm{T}$ & $[0.11792, -0.02554, 0.13616]^\mathrm{T}$ \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table}[htbp]
\centering
\bicaption{XCZU19EG 平台 ICM-20948 IMU 标定参数}{ICM-20948 IMU calibration parameters on the XCZU19EG platform}
\label{tab:xczu19eg_imu_calib}
\scriptsize
\begin{tabular}{lcc}
\toprule
参数项 & 平均值 & 单位 \\
\midrule
陀螺仪噪声密度 $gyr\_n$ & $1.9172 \times 10^{-3}$ & rad/s$/\sqrt{\mathrm{Hz}}$ \\
陀螺仪随机游走 $gyr\_w$ & $8.9883 \times 10^{-5}$ & rad/s$^2/\sqrt{\mathrm{Hz}}$ \\
加速度计噪声密度 $acc\_n$ & $2.0458 \times 10^{-2}$ & m/s$^2/\sqrt{\mathrm{Hz}}$ \\
加速度计随机游走 $acc\_w$ & $6.6290 \times 10^{-4}$ & m/s$^3/\sqrt{\mathrm{Hz}}$ \\
\bottomrule
\end{tabular}
\end{table}

\section{评价指标}

为全面刻画软硬协同加速器在精度、实时性与资源开销上的综合表现，本文从轨迹误差、计算延迟、能效与硬件资源四个维度给出评价指标。上述指标覆盖了视觉 SLAM 系统在实际部署中最关键的性能需求：定位精度、实时响应能力以及在资源受限平台上的可实施性与能耗成本。

本文采用以下指标对系统性能进行综合评估：

\textbf{（1）绝对轨迹误差（ATE）}

绝对轨迹误差用于衡量估计轨迹与真实轨迹之间的全局一致性，是评价视觉惯性系统定位精度的核心指标。由于不同算法在尺度与坐标系对齐上存在差异，本文在计算 ATE 前先进行刚体对齐，以保证误差统计具有可比性。
设估计位姿序列为 $\{\mathbf{P}_1, \mathbf{P}_2, \ldots, \mathbf{P}_n\}$，真实位姿序列为 $\{\mathbf{Q}_1, \mathbf{Q}_2, \ldots, \mathbf{Q}_n\}$。首先通过刚体变换 $\mathbf{S}$ 对齐：
\begin{equation}
\mathbf{S} = \arg\min_{\mathbf{S}} \sum_{i=1}^{n} \| \mathbf{Q}_i - \mathbf{S} \mathbf{P}_i \|^2
\label{eq:ate_alignment}
\end{equation}
对齐后，第 $i$ 帧位置误差为：
\begin{equation}
\mathbf{e}_i = \mathrm{trans}(\mathbf{Q}_i^{-1} \mathbf{S} \mathbf{P}_i)
\label{eq:ate_error}
\end{equation}
ATE 的均方根误差（RMSE）定义为：
\begin{equation}
\text{ATE}_{\text{RMSE}} = \sqrt{\frac{1}{n} \sum_{i=1}^{n} \| \mathbf{e}_i \|^2}
\label{eq:ate_rmse}
\end{equation}

\textbf{（2）计算延迟与加速比}

计算延迟反映单次滑动窗口后端优化的平均耗时，是衡量系统实时性的直接指标。为保证可比性，本文统一统计后端 Schur Kernel 的执行时间，并以 CPU 版本作为基准计算加速比，从而量化 FPGA 加速在端到端后端优化中的收益。
计算延迟 $T$ 表示处理单次滑动窗口优化所需的平均时间（ms）。加速比定义为：
\begin{equation}
\text{Speedup} = \frac{T_{\text{CPU}}}{T_{\text{FPGA}}}
\label{eq:speedup}
\end{equation}

\textbf{（3）能效}

能效指标用于评估单位计算任务的能量成本。本文以单帧能耗为度量，结合动态功耗与处理时延计算能量消耗，并以 CPU 方案为基准给出能效提升比，用于反映硬件加速在低功耗场景下的优势。
单帧能耗 $E$ 定义为：
\begin{equation}
E = P \times T
\label{eq:energy}
\end{equation}
其中 $P$ 为动态功耗（W），$T$ 为处理延迟（s）。能效提升比定义为 $E_{\text{CPU}} / E_{\text{FPGA}}$。

\textbf{（4）资源利用率}

硬件资源利用率用于评估加速器在 FPGA 平台上的实现代价，主要关注 LUT、FF、DSP 与 BRAM 的占用情况。该指标反映了硬件设计的可部署性与扩展空间，并可为不同平台之间的资源可行性对比提供依据。
FPGA 资源利用率包括 LUT、FF、BRAM 和 DSP 的占用情况，用于评估设计的硬件开销与可部署性。

\section{轨迹精度分析}

\begin{table}[htbp]
  \centering
  \bicaption{TUM-VI 数据集上的系统级 ATE 精度对比（单位：m）}{System-level ATE (m) on TUM-VI sequences}
  \label{tab:ate_tumvi}
  \scriptsize
  \begin{tabular}{lcccccccccccc}
    \hline
    Method              & c1 & c2 & c3 & c4 & c5 & r1 & r2 & r3 & r4 & r5 & r6  & Avg \\
    \hline
    SV(baseline)        & \textbf{0.329} & \textbf{0.285} & 0.555 & 0.162 & 0.274 & 0.048 & \textbf{0.160} & \textbf{0.066} & \textbf{0.049} & \textbf{0.054} & \textbf{0.021} & 0.182 \\
    SV+Schur Kernel   & 0.410 & 0.340 & \textbf{0.227} & \textbf{0.113} & \textbf{0.098} & \textbf{0.035} & 0.165 & 0.068 & 0.063 & 0.080 & 0.072 & \textbf{0.152} \\
    \hline
  \end{tabular}
  \begin{flushleft}
  \footnotesize
  Note 1: c1-c5 denote corridor1-corridor5 sequences. \\
  Note 2: r1-r6 denote room1-room6 sequences.
  \end{flushleft}
\end{table}

\begin{figure*}[!t]
  \centering
  \includegraphics[width=0.9\linewidth]{Img/tumvi-corridor2}
  \bicaption{TUM-VI 数据集走廊场景下的建图与定位轨迹结果}{Mapping and localization trajectory results in the corridor scene of the TUM-VI dataset}
  \label{fig:tumvi_corridor}
\end{figure*}

\begin{table}[htbp]
\centering
\bicaption{EuRoC 数据集轨迹精度对比（ATE RMSE，单位：m）}{Trajectory accuracy on EuRoC dataset (ATE RMSE, unit: m)}
\label{tab:trajectory_accuracy}
\begin{tabular}{lccc}
\toprule
序列 & CPU & FPGA \\
\midrule
MH\_01\_easy & 0.049 & \textbf{0.042} \\
MH\_02\_easy & 0.077 & \textbf{0.034} \\
MH\_03\_medium & \textbf{0.086} & 0.127 \\
MH\_04\_difficult & 0.125 & \textbf{0.113} \\
MH\_05\_difficult & 0.125 & \textbf{0.098} \\
V1\_01\_easy & \textbf{0.035} & \textbf{0.035} \\
V1\_02\_medium & \textbf{0.053} & 0.065 \\
V1\_03\_difficult & 0.082 & \textbf{0.068} \\
V2\_01\_easy & \textbf{0.046} & 0.063 \\
V2\_02\_medium & \textbf{0.075} & 0.080 \\
\midrule
\textbf{Avg} & \textbf{0.075} & \textbf{0.073} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure*}[!t]
\centering
% \begin{minipage}[b]{0.32\textwidth}
%   \centering
%   \begin{tikzpicture}[spy using outlines={circle, magnification=2.5, size=1.2cm, connect spies, every spy on node/.style={draw=red, line width=0.6pt}, every spy in node/.style={draw=red, line width=0.6pt}}]
%     \node[inner sep=0] (img) {\includegraphics[width=\textwidth]{Img/V2_01_easy_traj_trajectories.pdf}};
%     \spy on (2.2,1.8) in node at (1.9,-1.4);
%     \spy on (0.2,2.0) in node at (-0.1,1);
%   \end{tikzpicture}
%   \centerline{(a)}
% \end{minipage}
% \hfill
% \begin{minipage}[b]{0.32\textwidth}
%   \centering
%   \begin{tikzpicture}[spy using outlines={circle, magnification=2.5, size=1.2cm, connect spies, every spy on node/.style={draw=red, line width=0.6pt}, every spy in node/.style={draw=red, line width=0.6pt}}]
%     \node[inner sep=0] (img) {\includegraphics[width=\textwidth]{Img/V2_02_medium_traj_trajectories.pdf}};
%     \spy on (0.7,-0.6) in node at (1.5,-1.3);
%     \spy on (0.3,1.8) in node at (-0.3,1);
%   \end{tikzpicture}
%   \centerline{(b)}
% \end{minipage}
% \hfill
% \begin{minipage}[b]{0.32\textwidth}
%   \centering
%   \begin{tikzpicture}[spy using outlines={circle, magnification=2.5, size=1.2cm, connect spies, every spy on node/.style={draw=red, line width=0.6pt}, every spy in node/.style={draw=red, line width=0.6pt}}]
%     \node[inner sep=0] (img) {\includegraphics[width=\textwidth]{Img/MH_05_difficult_traj_trajectories.pdf}};
%     \spy on (-0.4,-1) in node at (1.5,1.3);
%     \spy on (2.4,-1.8) in node at (-1.3,-1);
%   \end{tikzpicture}
%   \centerline{(c)}
% \end{minipage}
\includegraphics[width=0.90\linewidth]{Img/euroc}
\bicaption{EuRoC数据集代表性序列轨迹对比。虚线为真值轨迹，实线为本文软硬协同方案估计轨迹。(a) V2\_01\_easy，ATE RMSE = 0.063\,m；(b) V2\_02\_medium，ATE RMSE = 0.080\,m；(c) MH\_05\_difficult，ATE RMSE = 0.098\,m}{Trajectory comparison on representative EuRoC sequences. Dashed: ground truth; solid: proposed SW/HW co-design. (a) V2\_01\_easy, ATE RMSE = 0.063\,m; (b) V2\_02\_medium, ATE RMSE = 0.080\,m; (c) MH\_05\_difficult, ATE RMSE = 0.098\,m}
\label{fig:euroc_trajectories}
\end{figure*}

为验证硬件加速不会引入额外的数值误差，本文在 EuRoC MAV 与 TUM-VI 数据集上对比了纯软件实现与软硬协同实现的轨迹精度。实验结果分别如表~\ref{tab:trajectory_accuracy} 和表~\ref{tab:ate_tumvi} 所示。

从表~\ref{tab:trajectory_accuracy} 可以看出，FPGA 加速器在 EuRoC 数据集各测试序列上的轨迹精度与纯软件基准基本一致（平均 ATE 分别为 0.073 m 与 0.075 m），误差差异处于数值噪声范围内。图~\ref{fig:euroc_trajectories} 进一步展示了三个代表性序列的轨迹对比结果（虚线为真值，实线为估计轨迹）。可以看到，无论是简单场景（V2\_01\_easy）、中等场景（V2\_02\_medium）还是困难场景（MH\_05\_difficult），FPGA 加速方案均能准确跟踪真实轨迹，验证了本文硬件实现的数值正确性。

同样，表~\ref{tab:ate_tumvi} 在 TUM-VI 数据集上的结果显示，软硬协同加速方案的平均 ATE 为 0.152 m，优于纯软件基准的 0.182 m，且在多数序列上表现更优。图~\ref{fig:tumvi_corridor} 展示了系统在 TUM-VI 走廊序列下的实时建图与定位轨迹结果，可以看到，即便在长距离走廊这种具有挑战性的环境下，系统依然能够稳定构建精确的特征地图并保持平滑的轨迹估计。这些结果表明，硬件加速器在显著提高计算效率的同时，不仅未牺牲定位精度，在部分场景下甚至因数值稳定性的改善而略有提升。

\section{计算延迟与加速比}

\subsection{算法效率对比}

为评估方法在不同算法与实现条件下的效率表现，本文在同一实验平台上设计两类对比：其一为不同 SLAM 方法的横向效率对比，其二为同一方法在引入 Schur Kernel 前后的纵向对比。横向对比以 CPU 版本为基准，重点对比运行帧率、CPU 平均利用率、整体功耗与精度指标（ATE RMSE）；ATE RMSE 采用 EuRoC MH\_01\_easy 序列进行测试。结果显示，SV+Schur Kernel 在帧率、CPU 占用与功耗方面均优于其他参照方法，同时保持更低的 ATE RMSE；SchurVINS 在整体效率上次之。相比 SVO 2.0 与 VINS-Fusion，OpenVINS 在帧率与功耗上更优，但精度略逊于 SchurVINS 系列。如图~\ref{fig:efficiency_barplots} 所示，四项指标的横向对比直观呈现了各方法的综合效率差异。

\begin{figure}[h!t]
\centering
\resizebox{0.95\linewidth}{!}{\input{Img/efficiency_barplots.tex}}
\bicaption{不同 SLAM 方法横向效率指标对比}{Horizontal efficiency comparison across different SLAM methods}
\label{fig:efficiency_barplots}
\end{figure}

纵向对比以 SchurVINS 为基准，将其后端优化模块替换为本文 Schur Kernel，评估后端与系统级加速幅度。选取 EuRoC 数据集的 MH\_01\_easy 序列进行测试，实验数据如表~\ref{tab:performance} 所示。Schur Kernel 的硬件实现将单次优化延迟从 2.84~ms 降低至 0.76~ms，实现 \textbf{3.74$\times$} 的加速比。得益于观测处理流水线与 SOB 编码优化，系统后端优化频率可稳定运行在 \textbf{25~Hz 以上}，满足实时性需求。

\begin{table}[htbp]
\centering
\bicaption{CPU 与 FPGA 后端耗时对比}{Latency comparison between CPU and FPGA backend}
\label{tab:performance}
\begin{tabular}{lccc}
\toprule
模块 & CPU 耗时 (ms) & FPGA 耗时 (ms) & 加速比 \\
\midrule
Schur Kernel & 2.84 & \textbf{0.76} & \textbf{3.74$\times$} \\
\bottomrule
\end{tabular}
\end{table}

\section{资源利用率分析}

表~\ref{tab:resource_comparison} 展示了本文与同类 FPGA 加速方案的资源占用与能效对比。本文仅将后端 Schur Kernel（Hessian 构建 + Schur 消元）部署于 PL，其余系统控制与前端仍由 PS 端完成（包括滑动窗口管理、特征跟踪/匹配、状态预测与优化迭代控制等）；对比表中的外部工作均为同类后端或 BA/SLAM 加速器在 FPGA 上的实现，统计范围为 FPGA 芯片上资源占用。表中 Liu Q~\cite{liu2022cicc}、Liu Q~\cite{liu2020pi}、Liu W~\cite{liu2021archytas} 与 Guo R~\cite{guo2022fpga} 均属于优化法后端方案，而 Gan Y~\cite{gan2021eudoxus} 为滤波法后端方案；本文在问题建模上采用优化法，在问题求解上采用滤波法的 EKF 更新，实现了优化建模与滤波求解的结合。优化法通常通过构建全局代价函数并求解批量最小二乘问题，而滤波法则以递推更新方式进行状态估计并强调线性化的逐步更新特性。由于软硬分工边界不同，资源规模存在差异，本文重点强调后端核心计算的协同设计与资源效率，尤其在 BRAM 占用方面显著优于同类工作，这得益于 SOB 编码对存储空间的优化。

为进一步体现能效优势，表中统计了帧率、功耗与单帧能耗（功耗/帧率）。其中帧率与功耗数据均在仅运行后端方法的条件下测得，反映该后端模块在对应平台上的极限性能。单帧能耗反映处理一帧所需能量，能够抵消不同平台帧率差异带来的偏置。可以看到，本文方案在保持较高帧率的同时，单帧能耗最低（35.6~mJ），说明本文方案在能效方面优势更为突出。值得注意的是，本文方案帧率显著高于其他方案，这不仅源于硬件流水线的高效设计，还得益于算法层面的轻量化优势：本文采用 Hessian 构建 + Schur 消元 + EKF 更新的单步求解策略，每帧仅需一次线性化与状态更新即可完成后端优化；而对比方案多采用 Levenberg-Marquardt（LM）等迭代优化算法，每帧需执行多轮迭代直至收敛，计算量随迭代次数线性增长。这种算法结构上的差异使得本文方案在单帧计算量上天然更低，与 FPGA 定制化流水线的结合进一步放大了帧率优势。

\begin{table*}[!t]
\centering
\bicaption{与同类 FPGA 后端加速方案深度对比}{In-depth comparison with peer FPGA backend accelerators}
\label{tab:resource_comparison}
\begin{tabular}{lcccccc}
\toprule
资源类型 & Liu Q \cite{liu2022cicc} & Gan Y \cite{gan2021eudoxus} & Liu Q \cite{liu2020pi} & Liu W \cite{liu2021archytas} & Guo R \cite{guo2022fpga} & \textbf{Ours} \\
\midrule
LUT  & 144108 & 350671 & 118995 & 136432 & 23489  & 114305 \\
FF   & 172935 & 239347 & 142411 & 163006 & 22696  & 155211 \\
DSP  & 869    & 1284   & 880    & 849    & 238    & 579    \\
BRAM & 1206 KB & 5787 KB & 2337 KB & 1149 KB & 230 KB & \textbf{67 KB} \\
帧率  & 60.86 FPS & 22.40 FPS & 4.00 FPS & 50 FPS & 83.3 FPS & 191 FPS \\
功耗  & 3.45 W & 8.96 W & 5.50 W & 5.00 W & N/A & 6.45 W \\
单帧能耗  & 56.7 mJ & 400 mJ & 1375 mJ & 100 mJ & N/A & \textbf{35.6 mJ} \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{不同硬件平台加速方案对比}

为全面评估本文 FPGA 加速方案的技术定位与优势，本节对比分析了面向 SLAM 后端优化的不同硬件加速平台。为实现公平的同算法跨平台对比，本文将 Hessian 构建与 Schur 消元模块使用 CUDA 实现，并部署于 NVIDIA Jetson TX2 开发板，与 FPGA 方案在相同算法、相同数据下进行性能与功耗的直接对比。此外，还与专用 ASIC 芯片 SLAM-CIM\cite{Li2024SLAMCIM}（同为后端加速方案）进行参考性对比，从硬件特性、性能能效、精度表现以及算法适配性等维度揭示不同硬件架构的权衡特点。

\subsubsection{硬件平台特性对比}

表~\ref{tab:platform_comparison} 总结了三类方案的核心硬件特性。本文 FPGA 方案基于 Xilinx Zynq UltraScale+ 异构平台，采用 PS-PL 协同架构，兼具灵活性与并行计算能力；GPU 对比方案将本文相同的 Hessian 构建与 Schur 消元算法使用 CUDA 实现，部署于 NVIDIA Jetson TX2 平台，利用 256 核 Pascal GPU 进行并行计算；ASIC 方案\cite{Li2024SLAMCIM}则采用 28nm CMOS 工艺实现存算一体（Compute-In-Memory, CIM）架构，专门针对 SLAM 后端的矩阵运算进行定制优化。

\begin{table*}[h!t]
\centering
\bicaption{不同硬件平台特性对比}{Comparison of hardware platform characteristics}
\label{tab:platform_comparison}
\scriptsize
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lccc}
\toprule
特性 & FPGA (本文) & GPU (CUDA) & ASIC\cite{Li2024SLAMCIM} \\
\midrule
硬件平台 & Zynq UltraScale+ & Jetson TX2 & 28nm CMOS \\
处理器 & ARM A53 + PL & Denver 2 + ARM A57 & 定制处理器 \\
计算单元 & 流水线+DSP & 256 CUDA核心 & CIM宏阵列 \\
工作频率 & 250 MHz (PL) & 1.3 GHz (GPU) & 12.5--300 MHz \\
工艺节点 & 16nm FinFET & 16nm FinFET & 28nm CMOS \\
加速目标 & Hessian+Schur & Hessian+Schur & 后端优化 \\
\bottomrule
\end{tabular*}
\end{table*}

从架构特点来看，FPGA 具有可重构性，支持针对特定算法的深度定制优化（如本文的 SOB 编码与五级流水线设计），同时保持了算法迭代的灵活性；GPU 依赖通用的 SIMT（Single Instruction Multiple Threads）架构，适合大规模数据并行任务，但在 Schur 消元等非规则稀疏计算中难以充分利用硬件并行度；ASIC 则通过硬件固化获得较高能效，但算法一旦流片后难以修改，开发周期较长且工程成本较高。

\subsubsection{性能、能效与精度对比}

表~\ref{tab:performance_comparison} 对比了三类方案的性能与能效表现。本文 FPGA 方案的 Schur Kernel 单帧延迟为 0.76 ms，相比 ARM CPU 实现 3.74 倍加速，整体后端处理帧率可达 191 FPS。在相同算法的 CUDA 实现中，Jetson TX2 的后端处理延迟与帧率如表~\ref{tab:performance_comparison} 所示。作为同类后端加速方案，ASIC 方案\cite{Li2024SLAMCIM}针对固定规模问题（192$\times$192 稀疏矩阵）实现了 2.8 ms 的处理延迟（约 357 FPS），但其适用场景受限于芯片设计时预定义的参数规模。由于本文 FPGA 方案与 GPU CUDA 方案实现了相同的算法模块并在相同的 EuRoC 数据集上进行测试，二者之间的对比可以直接反映 FPGA 与 GPU 在该类非规则稀疏计算任务上的平台特性差异。

\begin{table*}[h!t]
\centering
\bicaption{不同加速方案性能与能效对比}{Performance and energy efficiency comparison}
\label{tab:performance_comparison}
\scriptsize
\begin{tabular}{lccc}
\toprule
指标 & FPGA (本文) & GPU (CUDA) & ASIC\cite{Li2024SLAMCIM} \\
\midrule
加速目标 & Hessian+Schur & Hessian+Schur & 后端优化 \\
后端处理帧率 & 191 FPS & 87 FPS & \textbf{357 FPS} \\
动态功耗 & 6.80 W & 19.2 W & 35.53 W \\
单帧能耗 & \textbf{35.6 mJ} & 220.6 mJ & 99.5 mJ \\
加速比 (vs CPU) & 3.74$\times$ & 1.4$\times$ & \textbf{5.8$\times$} \\
\bottomrule
\end{tabular}
\end{table*}

在后端加速性能方面，本文 FPGA 方案的后端处理帧率为 191 FPS，虽低于 ASIC 方案的 357 FPS，但在单帧能耗上具有明显优势。本文 FPGA 方案的单帧能耗为 35.6 mJ，显著低于 ASIC 方案的 99.5 mJ。ASIC 方案帧率较高的主要原因在于其采用存算一体架构对固定规模矩阵运算进行了深度硬件固化，但这也牺牲了算法灵活性。尽管 ASIC 在峰值算力能效上达到 53.3 TFLOPS/W，但其系统级能效（包含数据搬运与控制逻辑）降至 0.182 TFLOPS/W\cite{Li2024SLAMCIM}，且 35.53 W 的动态功耗限制了其在移动机器人等功耗敏感场景下的应用。对于 GPU CUDA 方案，Jetson TX2 平台的功耗与处理延迟的具体对比结果详见表~\ref{tab:performance_comparison}，该对比能够直接体现相同算法在不同硬件架构上的效率差异。相比之下，本文 FPGA 方案通过 SOB 编码与动态门控机制，在保持较低功耗（6.80 W）的同时实现了较优的后端处理效率，体现了面向算法特性的定制化硬件设计在嵌入式场景下的优势。

\subsubsection{算法适配性与灵活性对比}

表~\ref{tab:flexibility_comparison} 从算法适配性与开发灵活性角度对比了三类方案。FPGA 方案支持在线重构与算法迭代，可通过修改 RTL/HLS 代码适配不同的 SLAM 算法与问题规模；GPU 方案依赖 CUDA 等通用并行编程框架，算法移植相对便捷，但对 Schur 消元等非规则稀疏运算的硬件利用率有限；ASIC 方案在流片后算法完全固化，难以适应 SLAM 算法从滤波法到优化法、从稀疏到稠密等持续演进过程。

\begin{table*}[h!t]
\centering
\bicaption{算法适配性与灵活性对比}{Comparison of algorithm adaptability and flexibility}
\label{tab:flexibility_comparison}
\scriptsize
\begin{tabular}{lccc}
\toprule
维度 & FPGA (本文) & GPU (CUDA) & ASIC\cite{Li2024SLAMCIM} \\
\midrule
算法可重构性 & 高 & 中 & 无 \\
问题规模适配 & 灵活 & 灵活 & 固定 \\
开发周期 & 3--6 个月 & 1--3 个月 & 12--24 个月 \\
NRE 成本 & 低 & 低 & 高（数百万美元） \\
单片成本 & 中 & 中 & 低（量产后） \\
软硬协同设计 & 支持 & 部分支持 & 不支持 \\
\bottomrule
\end{tabular}
\end{table*}

在软硬协同设计方面，本文 FPGA 方案通过 PS-PL 异构架构实现了算法层面的深度协同：将计算密集型的 Hessian 构建与 Schur 消元下沉到 PL 端，控制逻辑与 EKF 更新保留在 PS 端，避免了全系统硬化带来的灵活性损失。GPU 方案虽然也支持 CPU-GPU 协同，但其协同方式更多依赖主机端任务调度与通用并行计算框架，难以像 FPGA 那样对数据通路、存储访问和控制时序进行细粒度定制。ASIC 方案则完全依赖于设计阶段的算法选型与参数配置，一旦流片后无法适应算法演进或应用需求变化。

综合来看，FPGA 方案更适合功耗、实时性与算法灵活性并重的嵌入式后端优化场景；GPU 平台在通用并行计算中具有编程便捷的优势，但在 Schur 消元等非规则稀疏计算任务上效率受限，且功耗较高；ASIC 方案则更适用于算法相对稳定、具备大规模量产需求的场景。对于本文面向端侧 SLAM 后端加速的目标，FPGA 在单帧能耗、算法可重构性与开发成本之间取得了较为均衡的折中。

\section{功耗与能效分析}

为全面评估本文加速器的嵌入式部署优势，本文对 CPU 软件方案与 FPGA 硬件加速方案的功耗进行了实测对比。功耗测量采用开发板自带的电流传感器，分别记录系统在空闲状态与运行后端优化算法时的实时功率，结果如表~\ref{tab:power} 所示。由于硬件实现显著缩短了计算延迟，按照式~\eqref{eq:energy} 计算，单帧能耗显著降低，能效提升明显。

\begin{table}[htbp]
\centering
\bicaption{功耗对比}{Power consumption comparison}
\label{tab:power}
\begin{tabular}{lccc}
\toprule
实现方案 & 静态 (W) & 动态 (W) & 峰值 (W) \\
\midrule
ARM CPU (软件) & 6.53 & 7.04 & 7.10 \\
FPGA (硬件加速) & 6.53 & \textbf{6.80} & \textbf{6.95} \\
\bottomrule
\end{tabular}
\end{table}

\section{SOB 编码优化效果分析}

为量化稀疏观测位图（SOB）编码对系统性能的提升效果，本文对不同负载场景下的计算量、存储空间和数据传输量进行了详细分析。蒙特卡洛仿真是一种基于随机采样的统计方法，通过对随机变量进行大量重复试验，以估计系统在概率意义下的期望性能与波动范围\cite{metropolis1949monte,robert2004monte}。该方法适用于含有随机观测分布或复杂组合结构的问题，可在不穷举所有情况的前提下获得稳定的统计结论。

实验采用蒙特卡洛仿真方法：在给定 $(N_p, N_s, N_{\text{obs}})$ 配置下，随机生成满足观测数量约束的观测关联集合与 SOB 位图，并对每次随机样本计算对应的 Hessian 构建迭代次数、Schur 消元乘法次数与存储/传输开销，最终取 100 次样本的均值作为统计结果。该方法能够覆盖观测分布的随机性，避免单一轨迹或特定观测拓扑带来的偏差。稀疏率 $\rho$ 按式~\eqref{eq:sparsity_ratio} 定义，其中 $N_{\text{obs}}$ 为实际观测数，$N_p$ 为路标点数量，$N_s$ 为滑动窗口内的状态数，系数 2 对应双目相机。

\begin{equation}
\rho = \frac{N_{\text{obs}}}{N_p \times N_s \times 2}
\label{eq:sparsity_ratio}
\end{equation}

\begin{figure}[h!t]
    \centering
    \input{Img/sob_computation_chart.tex}
    \bicaption{SOB 编码计算量优化效果}{Computation reduction by SOB encoding}
    \label{fig:sob_computation}
\end{figure}

图~\ref{fig:sob_computation} 展示了 SOB 编码在不同稀疏率下的计算量优化效果。随着稀疏率增大，观测逐渐密集，优化带来的相对收益逐步降低，但在常见稀疏区间 40\%--60\% 仍保持显著优势。这一区间的观测分布既体现了稀疏性，又保留了足够数量的有效观测，使得跳零计算与有效负载之间取得更好的平衡。在典型场景（150 个路标点、600 个有效观测、稀疏率 50\%）下，Hessian 构建阶段的迭代次数从 1{,}200 次降至 600 次，节省 50\% 的计算量；舒尔消元阶段的 $\mathbf{H}_{pm} \times \mathbf{H}_{mm}^{-1}$ 矩阵乘法次数从 600 次降至 433 次，$\mathbf{H}_{pp}$ 矩阵更新次数从 2{,}400 次降至 1{,}401 次，综合节省约 40\% 的浮点运算。由于流水线中乘加与累加路径占比最高，该收益能够直接转化为后端时延的降低与吞吐提升。

SOB 编码对交叉项矩阵 $\mathbf{H}_{pm}$ 的存储空间和 DMA 传输量也有显著优化。由于每个有效的 $6 \times 3$ 子块占用 144 字节（18 个 FP64），跳过无效观测可直接减少存储和传输开销。表~\ref{tab:sob_summary} 汇总了典型场景下 SOB 编码的综合优化效果。通过协同优化计算、存储和传输三个维度，系统总浮点运算量从 1.687M 降至 924.3K，降低约 45\%，显著提升了硬件加速器的整体效率。

进一步地，从存储访问角度看，SOB 编码使得 Hessian 子块的写入由“固定稠密遍历”转为“按观测触发的稀疏写入”。在传统稠密写入策略下，硬件需对每个（状态，路标点）组合依次更新相应子块，即使该观测不存在也会触发无效写端口访问与写回，导致 BRAM 多端口冲突与写入仲裁开销增大。采用 SOB 编码后，写入行为仅在对应观测位为 1 时触发，写地址由位图解析即时生成，显著降低了并发写请求密度与端口冲突概率，从而改善了存储访问的时序收敛特性。与此同时，稀疏写入使得 DMA 输出呈“有效块连续”的流式特征，避免在总线端出现周期性峰值突发，有助于降低带宽峰值并提升传输稳定性。该特性在移动端平台尤为关键，可在相同带宽预算下维持更高的后端更新频率，并为前端传感器数据流预留充足的带宽裕量。此外，稀疏写入还降低了片上缓存的无效数据覆盖概率，使有效子块在缓存中的驻留时间更长，有利于提高缓存命中率并降低读写抖动，进一步提升流水线的稳定性与可预测性。

\begin{table}[htbp]
\centering
\bicaption{典型场景综合优化效果}{Overall optimization in typical scenario}\label{tab:sob_summary}
\begin{tabular}{lccc}
\toprule
优化维度 & 朴素方法 & SOB 优化 & 节省比例 \\
\midrule
雅可比计算浮点运算 & 901.2K & \textbf{450.5K} & \textbf{50.01\%} \\
Schur 消元浮点运算 & 785.4K & \textbf{473.8K} & \textbf{39.67\%} \\
$\mathbf{H}_{pm}$ 存储 & 84.38 KB & \textbf{60.95 KB} & \textbf{27.77\%} \\
DMA 输出传输 & 103.12 KB & \textbf{79.70 KB} & \textbf{22.72\%} \\
\midrule
\textbf{总浮点运算} & \textbf{1.687M} & \textbf{924.3K} & \textbf{45.18\%} \\
\bottomrule
\end{tabular}
\end{table}

\section{真实场景测试}

为验证本文加速方案在实际机器人系统中的作业能力，我们将系统部署于集成的移动机器人异构计算平台。如图~\ref{fig:VIOboard} 所示，该板卡搭载双目摄像头与 IMU 传感器，核心处理单元为 XCZU19EG，提供充足的 PS 与 PL 资源支持 Schur Kernel 加速架构。系统端到端流程包括：传感器同步采集、前端特征跟踪与匹配、滑动窗口状态管理、后端 Schur Kernel 计算以及结果回写与状态更新。为保证实验可复现性，测试过程中固定相机内参与外参标定参数，并对传感器时间戳进行对齐处理，确保视觉与惯性数据在同一时间基准下进入后端优化模块。

\begin{figure}[h!t]
\centering
\includegraphics[width=0.8\linewidth]{Img/VIOboard}
\bicaption{搭载双目摄像头与 IMU 的嵌入式开发板}{Embedded development board equipped with stereo cameras and IMU}
\label{fig:VIOboard}
\end{figure}

测试环境选择在户外真实场景进行，以验证系统在复杂动态环境下的定位与建图鲁棒性。测试场景涵盖了城市建筑、车辆、树林等多种复杂环境要素。如图~\ref{fig:outdoortest} 所示，系统在户外大场景中展现了优异的定位精度与建图一致性。

\begin{figure}[h!t]
\centering
\includegraphics[width=0.9\linewidth]{Img/outdoors_new.pdf}
\bicaption{户外真实场景下的定位与建图测试结果}{Localization and mapping results in an outdoor real-world scene}
\label{fig:outdoortest}
\end{figure}

% \begin{figure}[h!t]
% \centering
% \includegraphics[width=0.9\linewidth]{Img/outdoor}
% \bicaption{户外}{outdoor real-world scene}
% \label{fig:outdoor}
% \end{figure}

在处理 20~Hz 采集频率的双目观测流时，本文软硬协同加速架构展现了良好的作业潜力。实验结果显示，系统后端优化模块的计算频率可稳定在 \textbf{25~Hz 以上}，即便在长距离移动过程中也能保持稳定的实时响应。与此同时，后端计算的延迟波动较小，说明所提出的流水化与稀疏门控机制能够有效抑制由观测稀疏性变化带来的性能抖动。综合而言，真实场景实验验证了本文加速器在工程部署中的可行性与实时性，为后续面向移动机器人与嵌入式平台的应用奠定了基础。

\subsection{局限性与未来工作}

在与同类方案的对比方面，公开资料多集中于视觉前端或系统级加速，对后端 BA/Schur 求解的实现细节与配置参数披露有限，难以形成可直接对标的软硬件配置与数据规模对比。本文强调面向后端 Schur Kernel + EKF 增量更新的软硬协同路径，后续将结合更完整的公开实现或自建评测基准补充系统级对比。

本文设计的加速器采用 AXI-Lite/AXI-MM 标准接口与模块化 RTL 实现，具备在国产 SoC-FPGA 平台上的可迁移性：通过替换 DMA/DDR 控制器与调整时序约束即可完成总体适配，算法与流水线核心逻辑可保持不变。后续将开展国产平台的时序收敛与资源评估，量化不同国产器件下的频率与功耗边界。

\section{本章小结}

本章围绕所设计的 FPGA 软硬协同加速器，从实验环境搭建、传感器标定、算法精度、计算效率、硬件资源、功耗能效、SOB 编码优化与真实场景部署等多个维度对系统性能进行了系统性验证。

在实验环境方面，本章介绍了 ZCU106 评估平台与 XCZU19EG 应用平台的硬件配置与软件系统架构，并对 EuRoC MAV、TUM-VI 两个公开基准数据集进行了说明。针对真实平台，采用开源标定工具 Kalibr 完成了三阶段离线标定：以 pinhole-radtan 相机模型进行双目相机内外参标定（cam0 重投影误差 $0.97\,\mathrm{px}$，基线 $9.92\,\mathrm{cm}$），以 Kalibr 联合批量优化方法完成相机-IMU 外参与时间偏移标定（陀螺仪归一化残差 $1.34$，加速度计 $1.55$），并采用 Allan 方差分析提取 ICM-20948 的噪声密度与随机游走参数，为视觉惯性预积分提供了精准的噪声模型基础。

在算法精度方面，FPGA 加速器在 EuRoC 数据集上的平均 ATE 为 $0.073\,\mathrm{m}$，与纯软件基准（$0.075\,\mathrm{m}$）几乎持平，误差差异处于数值噪声范围内；在 TUM-VI 数据集上，软硬协同方案平均 ATE 为 $0.152\,\mathrm{m}$，优于纯软件基准的 $0.182\,\mathrm{m}$，在多数序列上数值稳定性略有改善。实验结果表明，硬件浮点流水线在显著提高吞吐的同时未引入额外的数值误差。

在计算效率方面，Schur Kernel 硬件实现将单次后端优化延迟从 $2.84\,\mathrm{ms}$ 压缩至 $0.76\,\mathrm{ms}$，实现 $\mathbf{3.74\times}$ 的加速比，系统后端优化频率稳定在 $\mathbf{25\,\mathrm{Hz}}$ 以上，满足实时 VIO 的运行要求。在资源利用率方面，本文方案 BRAM 占用仅为 $\mathbf{67\,\mathrm{KB}}$，显著低于同类 FPGA 后端加速方案（最低对比为 $230\,\mathrm{KB}$，最高达 $5787\,\mathrm{KB}$），这得益于 SOB 编码对 Hessian 稀疏子块存储的针对性优化。在功耗与能效方面，FPGA 方案动态功耗为 $6.80\,\mathrm{W}$，单帧能耗为 $\mathbf{35.6\,\mathrm{mJ}}$，优于所有参照方案。

在 SOB 编码优化效果方面，蒙特卡洛仿真定量分析表明，典型场景（稀疏率 $50\%$）下 SOB 编码可将总浮点运算量从 $1.687\,\mathrm{M}$ 降至 $924.3\,\mathrm{K}$，减少约 $\mathbf{45\%}$；$\mathbf{H}_{pm}$ 存储减少 $27.77\%$，DMA 传输减少 $22.72\%$。稀疏写入特性进一步改善了 BRAM 多端口冲突与总线带宽峰值，有助于提升流水线时序收敛性与传输稳定性。在真实场景部署方面，系统在户外复杂动态环境中的实测验证了软硬协同架构的工程可行性：后端在 $20\,\mathrm{Hz}$ 双目观测流下保持 $25\,\mathrm{Hz}$ 以上的稳定输出，延迟抖动较小，验证了稀疏门控机制对性能波动的有效抑制。综合各项实验结果，本文所设计的加速器方案在精度、实时性、资源效率与能效四个维度上均实现了预期目标，具备面向嵌入式移动平台的实际部署价值。