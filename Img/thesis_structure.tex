% thesis_structure.tex —— 论文结构安排横向树形图（匹配实际章节二级标题）
\usetikzlibrary{arrows.meta}
\begin{tikzpicture}[
    >=Stealth,
    every node/.style={font=\rmfamily\footnotesize},
    % 根节点
    root/.style={
        draw, rounded corners=4pt, thick, align=center,
        fill=blue!18, minimum width=24mm, minimum height=14mm,
        text width=22mm
    },
    % 章节节点
    chap/.style={
        draw, rounded corners=3pt, thick, align=center,
        fill=blue!8, minimum width=38mm, minimum height=8mm,
        text width=36mm, font=\rmfamily\footnotesize
    },
    % 内容叶节点
    leaf/.style={
        draw, rounded corners=2pt, thin, align=left,
        fill=gray!6, minimum width=46mm, minimum height=5mm,
        text width=44mm, font=\rmfamily\scriptsize,
        inner ysep=1.5pt
    },
    % 连线
    edge/.style={-, thick, gray!70},
    branch/.style={-, thin, gray!50},
]

%% ── 根节点 ───────────────────────────────────────────
\node[root] (root) at (0, 0)
    {\textbf{基于 FPGA 的}\\[-1pt]\textbf{SLAM 后端软硬协同}\\[-1pt]\textbf{加速器设计与实现}};

%% ── 章节节点 ────────────────────────────────────────
\node[chap] (c1) at (4.2,  7.0)  {\textbf{第一章}\ \ 绪\ \ 论};
\node[chap] (c2) at (4.2,  4.8)  {\textbf{第二章}\ \ 相关技术与理论基础};
\node[chap] (c3) at (4.2,  2.9)  {\textbf{第三章}\ \ 系统架构总体设计};
\node[chap] (c4) at (4.2,  0.4)  {\textbf{第四章}\ \ 后端加速器详细设计};
\node[chap] (c5) at (4.2, -3.6)  {\textbf{第五章}\ \ 系统实现与实验评价};
\node[chap] (c6) at (4.2, -7.0)  {\textbf{第六章}\ \ 总结与展望};

%% ── 根 → 章节连线 ──────────────────────────────────
\def\rx{2.0}

\draw[edge] (root.east) -- (\rx, 0);
\draw[edge] (\rx, 7.0) -- (\rx, -7.0);
\foreach \c/\cy in {c1/7.0, c2/4.8, c3/2.9, c4/0.4, c5/-3.6, c6/-7.0}{
    \draw[edge] (\rx, \cy) -- (\c.west);
}

%% ── 内容叶节点（叶间距 0.6cm）──────────────────────
\def\lx{9.6}
\def\mx{6.5}

% ── 第一章（4 叶，中心 7.0） ──
\node[leaf] (l1a) at (\lx,  7.9) {研究背景与意义};
\node[leaf] (l1b) at (\lx,  7.3) {国内外研究现状};
\node[leaf] (l1c) at (\lx,  6.7) {本文主要研究内容与贡献};
\node[leaf] (l1d) at (\lx,  6.1) {论文结构安排};

\draw[branch] (c1.east) -- (\mx, 7.0);
\draw[branch] (\mx, 7.9) -- (\mx, 6.1);
\draw[branch] (\mx, 7.9) -- (l1a.west);
\draw[branch] (\mx, 7.3) -- (l1b.west);
\draw[branch] (\mx, 6.7) -- (l1c.west);
\draw[branch] (\mx, 6.1) -- (l1d.west);

% ── 第二章（3 叶，中心 4.8） ──
\node[leaf] (l2a) at (\lx,  5.4) {视觉惯性里程计基础知识};
\node[leaf] (l2b) at (\lx,  4.8) {视觉测量模型与预处理};
\node[leaf] (l2c) at (\lx,  4.2) {视觉惯性状态估计与优化理论};

\draw[branch] (c2.east) -- (\mx, 4.8);
\draw[branch] (\mx, 5.4) -- (\mx, 4.2);
\draw[branch] (\mx, 5.4) -- (l2a.west);
\draw[branch] (\mx, 4.8) -- (l2b.west);
\draw[branch] (\mx, 4.2) -- (l2c.west);

% ── 第三章（3 叶，中心 2.9） ──
\node[leaf] (l3a) at (\lx,  3.5) {总体设计目标};
\node[leaf] (l3b) at (\lx,  2.9) {软硬件功能划分};
\node[leaf] (l3c) at (\lx,  2.3) {数据流分析与通信协议设计};

\draw[branch] (c3.east) -- (\mx, 2.9);
\draw[branch] (\mx, 3.5) -- (\mx, 2.3);
\draw[branch] (\mx, 3.5) -- (l3a.west);
\draw[branch] (\mx, 2.9) -- (l3b.west);
\draw[branch] (\mx, 2.3) -- (l3c.west);

% ── 第四章（5 叶，中心 0.4） ──
\node[leaf] (l4a) at (\lx,  1.6) {总体硬件架构};
\node[leaf] (l4b) at (\lx,  1.0) {雅可比计算模块设计};
\node[leaf] (l4c) at (\lx,  0.4) {SOB 编码与动态门控机制实现};
\node[leaf] (l4d) at (\lx, -0.2) {存储管理与 BRAM 优化};
\node[leaf] (l4e) at (\lx, -0.8) {Schur 核心模块实现};

\draw[branch] (c4.east) -- (\mx, 0.4);
\draw[branch] (\mx, 1.6) -- (\mx, -0.8);
\draw[branch] (\mx,  1.6) -- (l4a.west);
\draw[branch] (\mx,  1.0) -- (l4b.west);
\draw[branch] (\mx,  0.4) -- (l4c.west);
\draw[branch] (\mx, -0.2) -- (l4d.west);
\draw[branch] (\mx, -0.8) -- (l4e.west);

% ── 第五章（8 叶，中心 -3.6） ──
\node[leaf] (l5a) at (\lx, -1.5) {实验平台与环境搭建};
\node[leaf] (l5b) at (\lx, -2.1) {评价指标};
\node[leaf] (l5c) at (\lx, -2.7) {轨迹精度分析};
\node[leaf] (l5d) at (\lx, -3.3) {计算延迟与加速比};
\node[leaf] (l5e) at (\lx, -3.9) {资源利用率分析};
\node[leaf] (l5f) at (\lx, -4.5) {功耗与能效分析};
\node[leaf] (l5g) at (\lx, -5.1) {SOB 编码优化效果分析};
\node[leaf] (l5h) at (\lx, -5.7) {真实场景测试};

\draw[branch] (c5.east) -- (\mx, -3.6);
\draw[branch] (\mx, -1.5) -- (\mx, -5.7);
\draw[branch] (\mx, -1.5) -- (l5a.west);
\draw[branch] (\mx, -2.1) -- (l5b.west);
\draw[branch] (\mx, -2.7) -- (l5c.west);
\draw[branch] (\mx, -3.3) -- (l5d.west);
\draw[branch] (\mx, -3.9) -- (l5e.west);
\draw[branch] (\mx, -4.5) -- (l5f.west);
\draw[branch] (\mx, -5.1) -- (l5g.west);
\draw[branch] (\mx, -5.7) -- (l5h.west);

% ── 第六章（3 叶，中心 -7.0） ──
\node[leaf] (l6a) at (\lx, -6.4) {全文总结};
\node[leaf] (l6b) at (\lx, -7.0) {主要创新点};
\node[leaf] (l6c) at (\lx, -7.6) {后续工作展望};

\draw[branch] (c6.east) -- (\mx, -7.0);
\draw[branch] (\mx, -6.4) -- (\mx, -7.6);
\draw[branch] (\mx, -6.4) -- (l6a.west);
\draw[branch] (\mx, -7.0) -- (l6b.west);
\draw[branch] (\mx, -7.6) -- (l6c.west);

\end{tikzpicture}
